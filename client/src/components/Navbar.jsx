function Navbar() {
  return (
    <nav>
      <h2>LearnSphere</h2>

      <div>
        <a href="/">Home</a>
        <a href="/courses">Courses</a>
        <a href="/login">Login</a>
      </div>
    </nav>
  );
}

export default Navbar;

